// サンプルツールのスキル（Markdown 文字列・決定的）。
//
// 役割: get_current_time / calculate をいつ・どう使うかを Claude に教える。
//       現在日時などの揮発情報は書かない（安定プレフィックスとしてキャッシュされるため）。
// 関係: tools/example/index.js が skills に載せる。
export const EXAMPLE_SKILL = `# スキル: サンプルツール（時刻・計算）

このシェルに同梱されたサンプルの 2 ツール。ドメインのツールを足すときは、このファイルと tools/example/ を雛形にする。

## get_current_time
- 「今何時」「ニューヨークは今何時」「今日は何曜日」など、現在時刻に関する質問で使う。
- \`timezone\` は IANA 名（例: Asia/Tokyo, America/New_York, Europe/London）。都市名からタイムゾーンを推定して渡す。
- 返り値: \`{ iso, formatted, timezone, epochMs }\`。ユーザーには formatted を中心に伝える。

## calculate
- 数値を答えるときは暗算せず、必ずこのツールで計算する（単位換算・割合・べき乗も）。
- 使える記号: 数値と \`+ - * / % ^ ( )\` のみ。\`^\` はべき乗（右結合）。関数（sqrt など）や変数は使えないので、必要なら式を書き換える。
- 例: \`(12.5 + 3) * 4\` → 62、\`2 ^ 10\` → 1024、\`365 * 24 * 60 * 60\` → 31536000
- エラー（使えない文字・括弧の不整合・0 除算）はメッセージを読んで式を直し、再実行する。`
