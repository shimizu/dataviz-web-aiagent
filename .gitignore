# 依存・ビルド成果物
node_modules/
dist/
# viz-runtime（npm run build:runtime で生成。predev / prebuild からも作られる）
public/viz-runtime.js

# ログ・OS・エディタ
*.log
npm-debug.log*
.DS_Store
.vscode/
.idea/

# 環境変数（このアプリは使わないが念のため）
.env
.env.*
# 参照用にクローンした外部リポジトリ（コミットしない。必要なら submodule 化する）
reference/chart-visualization-skills/
